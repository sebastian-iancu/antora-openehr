// @overview
JSON Schema definitions for validating JSON-encoded openEHR data across all information models. Used by implementations to verify request and response payloads against the REST APIs.

// @spec:ITS-JSON-Overview
Overview of the openEHR JSON Schema suite. Describes schema organisation, versioning, and the mapping between JSON Schema definitions and the openEHR information model hierarchy.

// @spec:ITS-JSON
JSON Schema definitions for validating JSON-encoded openEHR data. Covers the Reference Model, Archetype Model, and BASE types used in REST API request and response payloads.
