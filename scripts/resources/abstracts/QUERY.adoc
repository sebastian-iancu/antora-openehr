// @overview
Archetype Query Language (AQL): an SQL-like language for retrieving openEHR data using archetype paths as the addressing mechanism. Includes the formal specification and annotated query examples.

// @spec:AQL
Archetype Query Language: an SQL-inspired query language for retrieving openEHR EHR data. Uses archetype paths as the addressing mechanism and supports SELECT, FROM, WHERE, ORDER BY, and aggregation functions for clinical data retrieval. AQL queries are submitted to openEHR platforms via the Query REST API, either as ad-hoc queries or as stored named queries with bound parameters. The language provides path-based navigation through Composition and Entry structures using archetype node identifiers, enabling precise targeting of clinical data elements.

// @spec:AQL_examples
Annotated AQL query examples covering common clinical retrieval patterns across EHR, Composition, Entry, and demographic contexts. Intended as a practical complement to the formal AQL specification. Examples range from simple single-archetype retrievals to complex multi-entry queries with joins, aggregations, and parameter substitution. Each example is accompanied by narrative explaining the clinical intent, the data model being queried, and notes on expected result sets.

// @micro:AQL
SQL-like language for querying EHR data using archetype paths as the addressing mechanism

// @micro:AQL_examples
Annotated examples covering common clinical data retrieval patterns
